# Frontend dependencies — Python 3.10.14 (pinned via runtime.txt)
# CRITICAL: pandas 2.1.4 has guaranteed prebuilt wheels for Python 3.10 (avoids 45+ min source builds)
# All versions below install in <2 minutes with zero C compilation.

streamlit==1.38.0
pandas==2.1.4
numpy==1.26.4
plotly==5.24.1
requests==2.32.3
pillow==10.4.0
python-dotenv==1.0.1
