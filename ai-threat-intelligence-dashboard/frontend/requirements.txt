# Frontend dependencies — Python 3.12 (pinned via runtime.txt at repo root)
# All packages below have prebuilt wheels for Python 3.12 — zero C compilation.

streamlit==1.38.0
plotly==5.24.1
pandas==2.2.3
numpy==1.26.4
pillow==10.4.0
requests==2.32.3
python-dotenv==1.0.1
